---
name: skillify
description: "[OMCP] Turn a repeatable workflow from the current session into a reusable omcp skill draft"
argument-hint: "<input>"
---

# /omcp:skillify

Turn a repeatable workflow from the current session into a reusable omcp skill draft

The skill at `skills/skillify/SKILL.md` defines the full procedure. Follow
that skill's instructions, using `{{ARGUMENTS}}` as the user input.

Task: {{ARGUMENTS}}
