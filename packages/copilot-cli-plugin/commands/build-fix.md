---
name: build-fix
description: "[OMCP] Fix build, typecheck, or compile errors with minimal changes until the build is green"
argument-hint: "<build command, error output, or 'auto-detect'>"
---

# /omcp:build-fix

Fix build, typecheck, or compile errors with minimal changes until the build is
green. Does not refactor, redesign, or add features.

The skill at `skills/build-fix/SKILL.md` defines the full procedure. Follow
that skill's instructions, using `{{ARGUMENTS}}` as the user input.

Task: {{ARGUMENTS}}
