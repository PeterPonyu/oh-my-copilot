---
name: release
description: "[OMCP] Generic release assistant — analyzes repo release rules, caches them in .omcp/RELEASE_RULE.md, then guides the release"
argument-hint: "<input>"
---

# /omcp:release

Generic release assistant — analyzes repo release rules, caches them in .omcp/RELEASE_RULE.md, then guides the release

The skill at `skills/release/SKILL.md` defines the full procedure. Follow
that skill's instructions, using `{{ARGUMENTS}}` as the user input.

Task: {{ARGUMENTS}}
