---
name: review
description: Severity-rated code review pass for pending changes or a specified diff.
argument-hint: "<input>"
---

# /omcp:review

Severity-rated code review pass for pending changes or a specified diff.

The skill at `skills/review/SKILL.md` defines the full procedure. Follow
that skill's instructions, using `{{ARGUMENTS}}` as the user input.

Task: {{ARGUMENTS}}
