# TODO_UNRESOLVED: git-master

OMC source not found at `/home/zeyufu/Desktop/references/oh-my-claudecode/skills/git-master/`.

`git-master` exists in OMC as an agent (in `agents/` or promoted agent), not as a skill with a `SKILL.md`.
It was listed in Wave 5-P1 as "NEW; promoted from agent" but no corresponding `skills/git-master/SKILL.md`
exists in the OMC reference repo.

**Action required**: Manually author `SKILL.md` for git-master based on the agent definition, or
wait for OMC upstream to publish a skill variant, then re-run the translator.
