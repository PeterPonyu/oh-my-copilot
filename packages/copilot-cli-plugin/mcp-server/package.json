{
  "name": "oh-my-copilot-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "oh-my-copilot-mcp": "./server.mjs"
  },
  "scripts": {
    "test": "node --test 'tests/*.test.mjs'",
    "build": "bash build.sh",
    "build:bundle": "esbuild server.mjs --bundle --platform=node --target=node18 --format=esm --outfile=dist/server.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
