---
name: setup
description: "[OMCP] Use first for install/update routing — sends setup, doctor, or MCP requests to the correct OMC setup flow"
argument-hint: "<input>"
---

# /omcp:setup

Use first for install/update routing — sends setup, doctor, or MCP requests to the correct OMC setup flow

The skill at `skills/setup/SKILL.md` defines the full procedure. Follow
that skill's instructions, using `{{ARGUMENTS}}` as the user input.

Task: {{ARGUMENTS}}
