---
name: docs-ship
description: Run repository docs checks and emit explicit evidence before completion.
argument-hint: "<input>"
---

# /omcp:docs-ship

Run repository docs checks and emit explicit evidence before completion.

The skill at `skills/docs-ship/SKILL.md` defines the full procedure. Follow
that skill's instructions, using `{{ARGUMENTS}}` as the user input.

Task: {{ARGUMENTS}}
