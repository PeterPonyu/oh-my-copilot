---
name: project-session-manager
description: "[OMCP] Worktree-first dev environment manager for issues, PRs, and features with optional tmux sessions"
argument-hint: "<input>"
---

# /omcp:project-session-manager

Worktree-first dev environment manager for issues, PRs, and features with optional tmux sessions

The skill at `skills/project-session-manager/SKILL.md` defines the full procedure. Follow
that skill's instructions, using `{{ARGUMENTS}}` as the user input.

Task: {{ARGUMENTS}}
