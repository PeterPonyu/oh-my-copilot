{
  "version": 1,
  "description": "Illustrative Copilot CLI hook policy for documentation-only checks. Not a runtime orchestration layer.",
  "hooks": [
    {
      "event": "session_start",
      "command": "echo 'oh-my-copilot example: keep v1 Copilot CLI-only and docs-first.'"
    }
  ]
}
